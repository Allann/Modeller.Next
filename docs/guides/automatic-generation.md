---
title: Automatic code generation on definition changes
description: Configure incremental generation during build.
---

# Automatic code generation on definition changes

The maintained setup is now part of the end-to-end onboarding path. Follow
[Automatic generation](/docs/getting-started/automatic-generation) after your
first manual `modeller generate --workspace .` succeeds.

That guide uses this version's `.modeller/config.json`, `*.modeller` RML inputs,
global or local .NET tool installation, and ownership-safe workspace generation.
