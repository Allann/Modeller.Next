# C# template catalogue

Each child directory is one independently selectable pack for a particular C#
purpose. `domain-project/` projects a context into a compiling domain model;
`api-project/` projects the same context into a compiling Minimal API host.
A future `dto-project/` pack can coexist without mixing recipes or templates
with either.
