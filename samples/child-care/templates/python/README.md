# Python template catalogue

Each child directory is one independently selectable pack for a particular
Python purpose. `api-project/` projects a complete canonical context into a
compiling FastAPI service, mirroring `csharp/api-project/` one-for-one at the
scope level while staying idiomatic to Python (snake_case identifiers,
Pydantic models, module-level FastAPI routers).
