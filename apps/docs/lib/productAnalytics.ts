'use client';
const EVENTS = new Set(['site_page_viewed', 'meaningful_use_started', 'outbound_link_followed', 'docs_article_viewed', 'docs_search_used', 'docs_call_to_action_selected']);
const PROPERTIES = new Set(['route', 'action']);
function readCookie(name: string) { return document.cookie.split('; ').find((item) => item.startsWith(`${name}=`))?.split('=')[1]; }
function distinctId() { const current = readCookie('modeller_analytics_id'); if (current) return current; const value = crypto.randomUUID(); document.cookie = `modeller_analytics_id=${value}; Max-Age=31536000; Path=/; Domain=.modeller.website; SameSite=Lax; Secure`; return value; }
export function capture(event: string, properties: Record<string, string | number | boolean> = {}) { const key = process.env.NEXT_PUBLIC_POSTHOG_KEY; if (!key || !EVENTS.has(event) || Object.keys(properties).some((name) => !PROPERTIES.has(name))) return; const host = (process.env.NEXT_PUBLIC_POSTHOG_HOST ?? 'https://us.i.posthog.com').replace(/\/$/, ''); void fetch(`${host}/capture/`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, keepalive: true, body: JSON.stringify({ api_key: key, event, properties: { ...properties, distinct_id: distinctId(), site: 'docs', environment: process.env.NEXT_PUBLIC_VERCEL_ENV ?? 'local', release: process.env.NEXT_PUBLIC_VERCEL_GIT_COMMIT_SHA ?? 'local', internal: readCookie('modeller_internal') === '1', contract_version: 1, $process_person_profile: false } }) }).catch(() => undefined); }
